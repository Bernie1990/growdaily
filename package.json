{
  "name": "growdaily",
  "version": "0.1.0",
  "description": "Japanese learning toolbook - growdaily",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html data/ dist/ && cp -r prompts/ dist/ 2>/dev/null || true",
    "deploy": "npm run build && wrangler pages deploy dist",
    "preview": "serve . -l 8080",
    "preview:wrangler": "wrangler pages dev ."
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "wrangler": "^4.90.0"
  }
}